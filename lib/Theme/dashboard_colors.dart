import 'package:flutter/material.dart';

/// Centralized color palette for the app's design system.
/// Use this class to ensure consistency across the UI.
class DashboardColors {
  DashboardColors._(); // Private constructor to prevent instantiation

  static const Color primary = Color(0xFF4E46B4);
  static const Color secondary = Color(0xFF10B981);
  static const Color warning = Color(0xFFF59E0B);
  static const Color events = Color.fromRGBO(52, 72, 240, 1);
  static const Color card = Colors.white;
  static const Color textPrimary = Color(0xFF111827);
  static const Color tabBackground = Color(0xFFF3F4F6);
  static const Color menuButtonBackground = Color.fromRGBO(250, 250, 250, 1);
  static const Color avatarBackground = Color.fromRGBO(250, 250, 250, 1);
  static const Color upcomingColor = Color.fromRGBO(226, 226, 226, 1);
}
